import ProbeHost from "@/components/live-probe/ProbeHost";

// Fase 0 (prueba técnica de tiempo real). Se borra cuando exista el módulo en vivo.
export default function VivoPruebaPage() {
  return <ProbeHost />;
}
